BACKEND_URL=http://localhost:4000
CONNECTOR_TOKEN=dev-connector-token
RECEIVER_PHONE=
# Номера, упоминание (@тег) которых в группе пропускает сообщение в инбокс.
# Несколько — через запятую. Формат любой: +7, пробелы, скобки.
GEM_MENTION_NUMBERS=
RECEIVER_ID=1
SESSION_DIR=./data/session
QUEUE_DIR=./data/queue
HEARTBEAT_INTERVAL_MS=15000
RETRY_BASE_MS=2000
